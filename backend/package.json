{
  "name": "mygamedb-backend",
  "version": "1.0.0",
  "description": "Backend API + WebSocket pour MyGameDB — gestion de collection de jeux vidéo",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "init-db": "node ../scripts/init-db.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.2.0",
    "ws": "^8.18.0"
  }
}
